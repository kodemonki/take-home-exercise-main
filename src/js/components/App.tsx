import React,{ useEffect } from "react";

function App() {

    useEffect(() => {
        //getSchedule().then((data: scheduleType[]) => setSchedule(data));

      }, []);

  return <h1>Hello world!</h1>;
}

export default App